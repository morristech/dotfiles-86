def sql(query)
  ActiveRecord::Base.connection.select_all(query)
end

projectrc = File.expand_path('config/.projectrc')
load projectrc if File.exist?(projectrc)
puts "Found and loaded .projectrc file"
